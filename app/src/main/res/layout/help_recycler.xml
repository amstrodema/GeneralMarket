<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardBackgroundColor="@android:color/transparent"
    android:layout_marginBottom="10dp"
    app:cardElevation="0dp"
    android:layout_marginHorizontal="5dp">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="1dp"
            android:id="@+id/helpRelative"
            android:paddingVertical="8dp"
            android:background="@drawable/btn_effect_curved_square">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/helpSubject"
                android:paddingHorizontal="5dp"
                android:paddingVertical="8dp"
                android:lineSpacingExtra="5dp"
                android:textColor="#000"
                android:layout_toLeftOf="@id/helpUpDownImgBtn"
                android:layout_alignParentLeft="true"/>
            <ImageButton
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:background="@android:color/transparent"
                android:id="@+id/helpUpDownImgBtn"
                android:src="@drawable/arrow_down"
                android:layout_centerVertical="true"
                android:layout_alignParentRight="true"/>

        </RelativeLayout>
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lineSpacingExtra="5dp"
            android:background="@drawable/fff_fill"
            android:id="@+id/helpMessage"
            android:textColor="#000"
            android:padding="5dp"
            android:visibility="gone"/>
    </LinearLayout>
</androidx.cardview.widget.CardView>